<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Terms & Policy - Find FIRST</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="/assets/css/main.css?v=54">
</head>
<body class="auth-page policy-page">
  <header></header>

  <main>
    <div class="container" style="max-width: 1180px; text-align: left;">
      <a href="/signup" class="btn btn-secondary back-button" data-terms-back-link>&larr; Back to signup</a>

      <div id="usercentrics-ppg" class="uc-privacy-policy" style="width: 100%; text-align: left;">
        <%- policyHtml %>
      </div>

      <script nonce="<%= cspNonce %>">
        (() => {
          try {
            const backLink = document.querySelector('[data-terms-back-link]');
            if (!backLink) return;
            const returnUrl = sessionStorage.getItem('signup_terms_return_url');
            if (returnUrl) backLink.setAttribute('href', returnUrl);
          } catch (e) {}
        })();
      </script>
    </div>
  </main>

  <script src="/assets/js/main.js?v=74"></script>
</body>
</html>
